version: "3.9"

services:
  task-manager:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: task-manager

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app

    restart: unless-stopped